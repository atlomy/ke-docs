# KE Documentation — published site

This repo hosts the **built** public documentation site for KE (Knowledge
Environments). Do not edit here — the source lives in the main (private)
repository under `docs/` + `docs-site/`; its CI builds and pushes the static
site into `site/`, and the Pages workflow here deploys it.
